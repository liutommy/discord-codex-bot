<!--
Default output style for every answer, written by the operator. Injected into the prompt as
<OUTPUT_STYLE> when this file has any content outside HTML comments. Leave empty to inject nothing.
Rebuild the image after editing: docker compose up -d --build
-->
